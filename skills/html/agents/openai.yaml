interface:
  display_name: "HTML"
  short_description: "Build clear visual explanations in HTML"
  default_prompt: "Use $html to turn this material into a clear, self-contained visual explanation."

policy:
  allow_implicit_invocation: true